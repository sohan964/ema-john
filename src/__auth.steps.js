/*

Auth setup;
    1. Create firebase project
    2. enable web
    3. enable sign in method
    4. install firebase
    5. get firebase config in firebase.config.js file
    6. export default app
*/

/*
    1. create UserContext( Auth Context) : Component name that provides AuthContext
    2. create AuthContext
    3 set AuthContext provider
    4. distracture the children on UserContext and write inside AuthContext.Provider
    5. export AuthContext to used inside useContext hook
    6. get form data
    7. getAuth in the UserContext


*/

/*
    firebase hosting
    **one time for a computer
        1. npm install -g firebase-tools
        2. firebase login

    **for every project
        1. firebase init
        2. make sure the public directory select build

    //for every deploy
    1. npm run build
    2. firebase deploy

*/